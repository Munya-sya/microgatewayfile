pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Running build automation'
                sh './micro-gw_HOME build petstore'
            }
        }
    }
}
